const initialState = [];

/*
  {prodId:  "1",
  amount: 3 };
*/

export default function cartSliceReducer(state = initialState, action) {
  switch (action.type) {
    case "XXXXXX":
      return state;

    default: {
      return state;
    }
  }
}
